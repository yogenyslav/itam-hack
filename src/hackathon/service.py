from bs4 import BeautifulSoup
import httpx

# resp = httpx.get(
#     "https://codenrock.com",
#     follow_redirects=True,
# )

# soup = BeautifulSoup(resp.text, "html.parser")
# print(resp.text)

# print(f"found: {soup.find_all('div', id_='index-contests')}")
# print(f"found: {soup.contents}")
